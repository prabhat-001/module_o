from django.contrib import admin
from .models import Guest
from .models import GuestPassport

# Register your models here.
admin.site.register(Guest)
admin.site.register(GuestPassport)

# Register your models here.
